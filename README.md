# WokiBrain

Restaurant table booking engine.

## Quick Start

```bash
npm install
npm run dev
```

Server starts on port 3000.

## Data

In-memory store loaded from `src/data/seed.json` on startup. Data resets when server restarts.
